# Implementation Plan - Project Structure and Modular Documentation Synchronization

Synchronize [doc/DOCUMENTATION.md](file:///d:/GitHub/[oU1TS]/b1t-Sched/doc/DOCUMENTATION.md) with the active modular codebase architecture established in v2.53.0+. This brings the "Project Structure", "Architecture", "JavaScript Modules", "Styling System", "User Flows", and "Views & Components" sections up to date with actual file paths, modern module groupings, and updated application logic.

## User Review Required

> [!IMPORTANT]
> The documentation in `doc/DOCUMENTATION.md` currently reflects a legacy flat `js/` structure, an outdated CSS listing, duplicate Classroom entries with misattributed CalendarView methods, and lacks newer features (Account Approvals, Event Raiders Feed, Changelog Modal, Faculty Classroom FAB, etc.).

No breaking application code changes are being made; this plan focuses exclusively on bringing [doc/DOCUMENTATION.md](file:///d:/GitHub/[oU1TS]/b1t-Sched/doc/DOCUMENTATION.md) into 100% alignment with the active codebase.

---

## Proposed Changes

### Documentation Updates: [doc/DOCUMENTATION.md](file:///d:/GitHub/[oU1TS]/b1t-Sched/doc/DOCUMENTATION.md)

#### 1. Architecture & Module Dependencies (Lines ~164–235)
- **Application Flow Diagram**: Update the diagram to depict the 5 modular layers:
  - **Core Layer** (`js/core/`): Firebase config, Auth, DB, Router, UI, Utils, App.
  - **Feature Modules** (`js/features/`): Profile, Classroom, Notices, Notes, Calendar, Approvals, Timeline, Event Raiders, Changelog.
  - **PWA & Offline** (`js/pwa/`): Detection, Manifest, Caching, Install Prompts, Offline Indicator & Queue.
  - **Notifications** (`js/notifications/`): Types, Content Formatter, Notification Manager.
  - **Admin & Maintenance** (`js/admin/`): Admin API, Activity Logger, Migrations, Semesters Update.
- **Module Dependencies Tree**: Update dependency mapping from flat paths to actual modular directory imports (`js/core/app.js` coordinating `js/core/*`, `js/features/*`, etc.).

#### 2. Project Structure (Lines ~237–328)
- Update the directory tree to represent the real repository:
  - **Root files**: `index.html`, `manifest.json`, `sw.js`, `inject-env.js`, `changes.json`, `cors.json`, `firebase.json`, `firestore.rules`, `firestore.indexes.json`, `package.json`, `README.md`, `CLAUDE.md`, `AGENTS.md`.
  - **`css/`**: All 18 active stylesheets (`buttons.css`, `calendar.css`, `changelog.css`, `classroom.css`, `colors.css`, `components.css`, `dashboard.css`, `drop-down.css`, `main.css`, `menu-bar.css`, `navbar.css`, `note.css`, `notice.css`, `responsive.css`, `selector.css`, `styles.css`, `timeline.css`, `user-details-card.css`).
  - **`js/`**: Exact 5-directory modular layout:
    - `core/`: `firebase-config.js`, `auth.js`, `db.js`, `ui.js`, `routing.js`, `utils.js`, `permission-manager.js`, `firestore-listener-manager.js`, `app.js`
    - `features/`: `profile.js`, `cr-notice.js`, `notice.js`, `classroom.js`, `facultyClassroom.js`, `notes.js`, `approvals.js`, `timeline-data.js`, `timeline-ui.js`, `task-export.js`, `changelog-modal.js`, `raids-feed.js`, `calendar-view.js`
    - `pwa/`: `cache-manager.js`, `install-prompt.js`, `manifest-generator.js`, `offline-indicator.js`, `offline-manager.js`, `pwa-detector.js`, `pwa-init.js`, `sw-update-manager.js`
    - `notifications/`: `notifications-types.js`, `notification-content-formatter.js`, `notification-manager.js`
    - `admin/`: `admin-api.js`, `activity-logger.js`, `migrate-activity-logs.js`, `update-user-semesters.js`
  - **`doc/`**: Full subdirectories (`prompts/`, `plan/`, `summaries/`) and supplementary markdown guides.
  - **`functions/`**: Cloud functions structure.

#### 3. JavaScript Modules Section (Lines ~329–1094)
- **Reorganize by Directory Category**:
  - Group into Core Modules, Feature Modules, PWA Modules, Notifications System, and Admin Modules.
  - Update all section headings with accurate file paths (e.g. `### 1. Firebase Configuration (js/core/firebase-config.js)`).
- **Fix CalendarView & Classroom Table Integrity**:
  - Move CalendarView methods (`close()`, `renderCalendar()`, `renderMonthlyViewMobile()`, `renderWeeklyView()`, etc.) back under `### CalendarView (js/features/calendar-view.js)`.
  - Remove duplicate, outdated Classroom section (formerly `### 13. Classroom (classroom.js)` at line 1006).
- **Document Missing Modules**:
  - `js/features/approvals.js` (Account Approvals management)
  - `js/features/changelog-modal.js` (In-app Changelog viewer with version ranges)
  - `js/features/raids-feed.js` (UITS Event Raiders feed syndication)
  - `js/features/facultyClassroom.js` (Faculty & CR Classroom assignment/announcement creation FAB)

#### 4. Styling System (Lines ~1102–1150)
- Update CSS architecture flowchart to include `calendar.css`, `changelog.css`, `note.css`, `timeline.css`, and `classroom.css`.
- Note color system themes: Default Maroon, Dark Mode (with blue accent `#3b82f6`), and Gray Mode.

#### 5. User Flows (Lines ~1498–1570)
- Update Registration & Login diagrams to reflect:
  - Student / Faculty segmented toggle.
  - Support for Student ID (10–16 digits) and Faculty Initial login alongside email.
  - Account Approvals workflow for newly registered faculty accounts awaiting admin verification.

#### 6. Views & Components & API Reference (Lines ~1574–1675)
- In **Views (Screens)** table, add `account-approvals-view` (`#/account-approvals`).
- In **Modals list**, add Changelog Modal (`#changelog-modal`), Task Export Modal (`#task-export-modal`), Calendar Modal (`#calendar-modal`), Timeline Modal (`#timeline-modal`), and Account Approvals Modal (`#approvals-modal`).
- In **API Reference**, update global object inventory (`Auth`, `DB`, `UI`, `Router`, `Profile`, `Utils`, `App`, `Classroom`, `NoticeViewer`, `NoteManager`, `CalendarView`, `TimelineUI`, `TimelineData`, `TaskExport`, `ChangelogModal`, `EventRaidersFeed`, `AdminAPI`, `ActivityLogger`).

---

## Verification Plan

### Automated Verification
- Run a link integrity script across `doc/DOCUMENTATION.md` to guarantee that all Table of Contents anchors and internal links continue resolving with 100% pass rate.
- Validate that all listed file paths in the updated `Project Structure` exist in the repository on disk.

### Manual Verification
- Review the rendered markdown structure to verify visual readability, correct heading hierarchy, and absence of duplicate sections.
